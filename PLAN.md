# Issue #7 plan

- [x] Implement PL-gated `!mint` knock and signup codes (already on staging).
- [x] Implement `!codes` and `!revoke` (already on staging).
- [x] Implement PL-gated `!ban`, `!kick`, and `!unban` for the Shape Rotator space.
- [x] Implement `!stats` with 24-hour audit counts, pending flows, and top captcha keywords.
- [x] Record the encrypted-admin-room decision and mautrix requirement.
- [x] Document the co-admin handoff in `deploy/admin/README.md`.
